# Specify default set of spectra and scale cuts
# to be used
defaults:
  # Which spectra?
  polarizations: [TT]
  # Scale cuts (in ell) for each spectrum
  scales:
    TT: [30, 9000]

params: !defaults [calib, calib_T]
